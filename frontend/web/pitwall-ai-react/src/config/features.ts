// Flip this to switch the F1 globe visual between two modes:
// true  -> full-page fixed background behind all pages (current design)
// false -> original inline globe clipped inside the Home hero card
export const ENABLE_FULLPAGE_GLOBE = true
